# شناسنامه اثر — اپلیکیشن مالی Eghtesadino

---

## بخش ۱: اطلاعات کلی اثر

| ردیف | عنوان | توضیحات |
|------|-------|---------|
| ۱ | **عنوان پروژه** | Eghtesadino — اپلیکیشن مدیریت مالی دانش‌آموزان |
| ۲ | **شناسه گروه** | در حال تکمیل |
| ۳ | **حوزه کاربرد** | برنامه‌کاربردی (Practical Programming) |
| ۴ | **استان** | در حال تکمیل |
| ۵ | **شهرستان** | در حال تکمیل |
| ۶ | **نام مدرسه** | در حال تکمیل |
| ۷ | **نام و نام خانوادگی دانش‌آموزان عضو گروه** | در حال تکمیل |
| ۸ | **کد ملی** | در حال تکمیل |
| ۹ | **پایه تحصیلی** | در حال تکمیل |
| ۱۰ | **تلفن همراه** | در حال تکمیل |
| ۱۱ | **تلفن منزل** | در حال تکمیل |
| ۱۲ | **تلفن مدرسه** | در حال تکمیل |
| ۱۳ | **هدف پروژه** | ساخت اپلیکیشن دسکتاپ کمک‌کننده دانش‌آموزان برای مدیریت درآمد، هزینه، اهداف پس‌انداز و کسب بینش‌های مالی |

---

## بخش ۲: فازهای پروژه

### فاز ۱: تجزیه و تحلیل و نیازسنجی

| ردیف | عنوان | توضیحات |
|------|-------|---------|
| ۱ | **مشکل یا داستان پروژه** | دانش‌آموزان اغلب با درآمد و هزینه‌ای 混乱 دارند و نیاز به ابزار ساده برای نقدّر کردن مالی‌شان وجود دارد. اکثر راه‌حل‌های موجود پیچیده یا تجاری هستند. |
| ۲ | **هدف پروژه** | تولید اپلیکیشن دسکتاپ متن‌باز با رابط کاربری مدرن که به کاربران امکان می‌دهد تراکنش‌های مالی را ثبت کرده، اهداف پس‌انداز داشته باشند، گزارش‌های تحلیلی دریافت کنند و بازخوردهای هوشمند دریافت کنند. |
| ۳ | **مخاطبان** | دانش‌آموزان دبیرستانی و دانشجویان (سن ۱۵–۲۵ سال)، آموزگران و اولیا |
| ۴ | **نیازهای پروژه** | — ثبت‌نام و ورود امن با رمزنگاری پسورد — افزودن درآمد و هزینه با دسته‌بندی — نمایش موجودی و خلاصه مالی — ایجاد و پیگیری اهداف پس‌انداز — گزارش‌های ماهانه و تحلیلی — تغییر تم و تنظیمات — نکات آموزشی مالی |
| ۵ | **سفر مشتری** | کاربر اولین بار اپلیکیشن را اجرا می‌کند ← صفحه ورود/ثبت‌نام ← وارد داشبورد ← افزودن تراکنش ← مشاهده گزارش‌ها ← تنظیمات ← خروج |
| ۶ | **ارزش پیشنهادی** | صرفه‌جویی در زمان با نقدّر خودکار مالی، اعلان خودکار تراکنش‌ها، و توصیه‌های مالی شخصی‌سازی‌شده |
| ۷ | **مستندات اضافی** | مقایسه با اپلیکیشن‌های مشابه: جایگزین رایگان و متن‌باز برای ابزارهای مدیریت مالی تجاری مثل Mint و Wallet |

### فاز ۲: طراحی

| ردیف | عنوان | توضیحات |
|------|-------|---------|
| ۱ | **ساختار پروژه** | `main.py` (مرورگر صفحات) ← `models/` (منطق کسبی و دیتابیس) ← `screens/` (رابط کاربری) ← `utils/` (ابزارهای مشترک). الگو MVC ساده‌شده. |
| ۲ | **فلوچارت اتصال بخش‌ها** | `main.py` → `FinancialApp` → `LoginScreen` → `DashboardScreen` → `ReportsScreen` / `AddGoalScreen` / `SettingsScreen`. هر صفحه مولد بوده و از طریق `pack`/`pack_forget` نمایش/مخفی می‌شود. |
| ۳ | **طراحی گرافیک** | رابط کاربری مدرن با حالت تاریک/روشن، مقیاس‌پذیر بر اساس DPI، کارت‌های گرد با رنگ‌های هماهنگ. ابزار طراحی: CustomTkinter + Python. |
| ۴ | **ساختار داده** | SQLite دیتابیس محلی با سه جدول: `users` (کاربران)، `transactions` (تراکنش‌ها)، `goals` (اهداف پس‌انداز). کلید خارجی `user_id` برای ایمنی داده. |

### فاز ۳: پیاده‌سازی

| ردیف | عنوان | توضیحات |
|------|-------|---------|
| ۱ | **ابزارها و زبان‌ها** | **زبان:** Python 3.10+ — **کتابخانه:** CustomTkinter (رابط کاربری) — **پایگاه داده:** sqlite3 (استاندارد) — **تکنولوژی:** دیتابیس SQLite محلی |
| ۲ | **پلتفرم** | دسکتاپ (ویندوز، لینوکس، مک) |
| ۳ | **رابط کاربری (UI/UX)** | پنج صفحه: ورود/ثبت‌نام، داشبورد اصلی، گزارش‌ها، افزودن هدف، تنظیمات. طراحی مینیمال با کارت‌ها، دکمه‌های رنگی، و انیمیشن‌های ساده |
| ۴ | **پایگاه داده** | SQLite دیتابیس محلی (`financial_data.db`). سه جداول: `users`، `transactions`، `goals`. مدیریت مهاجرت اسکیما برای سازگاری رو به عقب |
| ۵ | **الگوی رفتار کاربر** | کاربر ثبت‌نام می‌کند ← وارد حساب می‌شود ← تراکنش اضافه می‌کند ← اهداف پس‌انداز می‌سازد ← گزارش‌ها را مشاهده می‌کند ← تنظیمات را تغییر می‌دهد |
| ۶ | **بازخورد به کاربر** | پیام‌های تأیید (success/warning/error) با `messagebox`، اعلان‌های بودجه هوشمند، و بازخورد بصری برای پیشرفت اهداف |
| ۷ | **راهنمای کاربر** | فایل README.md شامل: نصب، استفاده، معماری، امنیت. رابط کاربری خودکار راهنماست |
| ۸ | **امنیت** | هش کردن رمز با PBKDF2-HMAC-SHA256 (۲۰۰,۰۰۰ تکرار)، رمزنگاری فیلد-level برای توضیحات و دسته‌بندی تراکنش‌ها، پارامتری‌سازی کوئری‌ها (جلوگیری از SQL Injection) |
| ۹ | **سخت‌افزار خاص** | ندارد |
| ۱۰ | **هوش مصنوعی** | اپلیکیشن از ابزارهای AI در بخش‌های مختلف کار استفاده نمی‌کند. هوش مصنوعی توصیه‌های مالی به صورت قانون‌محور (rule-based) ارائه می‌دهد |

### فاز ۴: تست

| ردیف | عنوان | توضیحات |
|------|-------|---------|
| ۱ | **نصب و سازگاری** | Python 3.10+ + CustomTkinter. بین پلتفرم‌ها (ویندوز/لینوکس/مک) قابل اجرا |
| ۲ | **رابط کاربری** | تست دستی صفحات ورود، داشبورد، گزارش‌ها، تنظیمات و افزودن هدف |
| ۳ | **عملکرد** | ثبت تراکنش، نقدّر کردن موجودی، محاسبه نسبت پس‌انداز، تشخیص مخارج تکراری |
| ۴ | **امنیت** | هش رمز، رمزنگاری داده، جلوگیری از SQL Injection |
| ۵ | **صحت محتوا** | محاسبه صحیح موجودی، گزارش‌های دقیق |
| ۶ | **بومی‌سازی** | مناسب برای دانش‌آموزان ایرانی با واحد ریال |

### فاز ۵: نگهداری

| ردیف | عنوان | توضیحات |
|------|-------|---------|
| ۱ | **رفع خطا** | کد منبع کاملاً در دسترس است. گزارش‌های خطا از طریق issue tracker GitHub |
| ۲ | **آپدیت** | به‌روزرسانی با git pull |
| ۳ | **فیدبک کاربر** | از طریق ایمیل یا issue tracker دریافت می‌شود |
| ۴ | **نوآوری** | قابلیت‌های جدید مانند اکسلپورت گزارش‌ها، پشتیبان‌گیری، چندوالوتی |

---

## بخش ۳: ابزارها و تکنولوژی‌ها

| ردیف | مورد | توضیحات |
|------|------|---------|
| ۱ | **زبان برنامه‌نویسی** | Python 3.10+ |
| ۲ | **فریم‌ورک رابط کاربری** | CustomTkinter (wrapper مدرن برای Tkinter) |
| ۳ | **پایگاه داده** | SQLite 3 (استاندارد Python) |
| ۴ | **هش رمز** | hashlib (PBKDF2-HMAC-SHA256) |
| ۵ | **رمزنگاری** | XOR stream cipher با کلید مشتق‌شده از PBKDF2 |
| ۶ | **طراحی UI** | CustomTkinter + سفارشی‌سازی رنگ و فونت |

---

## بخش ۴: امنیت

| ردیف | نکته امنیتی | توضیحات |
|------|-------------|---------|
| ۱ | **هش رمز عبور** | PBKDF2-HMAC-SHA256 با ۲۰۰,۰۰۰ تکرار و سالت تصادفی ۱۶ بایت |
| ۲ | **رمزنگاری داده** | تراکنش‌ها (دسته‌بندی و توضیحات) رمزنگاری شده با XOR stream cipher |
| ۳ | **جلوگیری از SQL Injection** | تمام کوئری‌ها پارامتری هستند |
| ۴ | **پیپر (Pepper)** | کلید رمزنگاری قابل پیکربندی از طریق متغیر محیطی |

---

## بخش ۵: نوآوری اثر

| ردیف | نوآوری | توضیحات |
|------|--------|---------|
| ۱ | **تمایز از نمونه‌های مشابه** | ترکیب مدیریت مالی شخصی + آموزش مالی + اهداف ذخیره در یک اپلیکیشن واحد |
| ۲ | **بازخورد هوشمند** | سیستم اعلان مبتنی بر الگوهای خرج (سرعت افزایش خرج، کاهش نرخ پس‌انداز) |
| ۳ | **رابط کاربری مدرن** | حالت تاریک/روشن، مقیاس‌پذیری DPI، طراحی کارتی تمیز |
| ۴ | **امنیت سطح فیلد** | رمزنگاری اطلاعات حساس تراکنش‌ها در سطح دیتابیس |
| ۵ | **قابلیت اطمینان** | مهاجرت خودکار اسکیما دیتابیس برای نسخه‌های قدیمی |

---

## بخش ۶: مستندات اضافی (اختیاری)

| ردیف | فایل | وضعیت |
|------|------|-------|
| ۱ | **فایل پروژه (سورس کد)** | پیوست شده — شامل تمام فایل‌های Python |
| ۲ | **فیلم ۵ دقیقه‌ای** | Video.mp4 — نمایش عملکرد اپلیکیشن |
| ۳ | **شناسنامه اثر** | پیوست شده — این فایل |
| ۴ | **مستندات اضافی** | شامل README.md با معماری، نحوه استفاده، و امنیت |

---

## بخش ۷: تکمیل شناسنامه اثر بر اساس ویژگی‌های زیرمحور «برنامه کاربردی»

### داوری: معیار ۱ — تهیه مستندات کلیه فازهای مهندسی (۱۰ امتیاز)
کلیه فازهای مهندسی نرم‌افزار (نیازسنجی، طراحی، پیاده‌سازی، تست، نگهداری) در فرآیند توسعه Eghtesadino رعایت شده است. مستندات شامل شناسنامه اثر این فایل، README.md، و کامنت‌های کد هستند.

### داوری: معیار ۲ — ارزش علمی و صحت اطلاعات (۵ امتیاز)
راهبردهای مالی (قانون ۲۰٪ پس‌انداز، تفکیک نیازها از دلایل) بر اساس اصول برنامه‌ریزی مالی دانشگاهی است. محاسبات مالی دقیق هستند و منابع علمی پایه استفاده شده شامل کتابخانه‌های استاندارد Python و روش‌های امنیتی توصیه‌شده توسط OWASP هستند.

### داوری: معیار ۳ — داشتن راهنما و مستندات (۵ امتیاز)
راهنمای کاربر در README.md شامل: نصب، استفاده از اپلیکیشن، معماری، ویژگی‌های امنیتی. کد منبع کامل ارائه شده. شبه‌کد (pseudocode) در کامنت‌های منطق تجاری موجود است.

### داوری: معیار ۴ — جذابیت نرم‌افزار و UI/UX (۱۰ امتیاز)
رابط کاربری مدرن با CustomTkinter، طراحی کارتی، حالت تاریک/روشن، مقیاس‌پذیری DPI، انیمیشن‌های ورود و کنترل‌های بصری هم‌رنگ. استفاده از آیکون‌های یونیکد و چیدمان تمیز.

### داوری: معیار ۵ — غیرمتمرکز (DApp) و قابلیت‌های بلوکچین (۵ امتیاز)
اپلیکیشن دسکتاپ محلی است و برای این زیرمحور نقطه قوت خاصی ندارد اما امنیت داده را با هش و رمزنگاری سطح فیلد بررسی می‌کند.

### داوری: معیار ۶ — مدیریت خطاها و اصول کدنویسی (۱۰ امتیاز)
- مدیریت خطا: هر عملیات دیتابیس در try/except بسته شده و rollback انجام می‌شود
- اصول کدنویسی نام‌گذاری واضح، توابع کوتاه و هدفمند، دسته‌بندی منطقی
- نام مستعار پیشوند زیرخط (underscore) برای توابع داخلی
- تکرار کد کاهش یافته با استفاده از توابع مشترک

### داوری: معیار ۷ — چندپلتفرمه بودن (۵ امتیاز)
اپلیکیشن روی هر سه پلتفرم اصلی (ویندوز، لینوکس، مک) قابل اجراست چون Python و CustomTkinter پلتفرم‌محور نیستند.

### داوری: معیار ۸ — استفاده از پایگاه داده و ابزارهای حرفه‌ای (۵ امتیاز)
- استفاده از SQLite با پارامتری‌سازی کوئری‌ها
- نرمال‌سازی جداول (جدول جداگانه برای کاربران، تراکنش‌ها، اهداف)
- مدیریت مهاجرت اسکیما برای سازگاری با نسخه‌های قدیمی دیتابیس
- connection pooling ساده (بستن اتصال در finally block)

### داوری: معیار ۹ — مقیاس‌پذیری (۵ امتیاز)
اپلیکیشن برای کاربران فردی طراحی شده اما قابلیت افزایش تعداد کاربران و تراکنش‌ها را دارد. ساختار داده‌ای SQLite قابل انتقال به PostgreSQL یا MySQL هست.

### داوری: معیار ۱۰ — قابلیت تجاری‌سازی (۵ امتیاز)
اپلیکیشن متن‌باز (MIT License) است و امکان تجاری‌سازی با نسخه Pro با قابلیت‌های اضافی (اکسلپورت گزارش‌ها، پشتیبان‌گیری، چندوالوتی) وجود دارد.

### داوری: معیار ۱۱ — مکانیسم‌های مناسب بازخورد کاربران (۵ امتیاز)
- پیام‌های تأیید ورود/ثبت‌نام
- اعلان‌های هوشمند بودجه بر اساس الگوهای خرج
- بازخورد بصری پیشرفت اهداف (% رسیدن به هدف)
- در آینده: فرم نظرسنجی از کاربران

### داوری: معیار ۱۲ — میزان خالقیت و نوآوری (۱۰ امتیاز)
ترکیب بازاریه ایده‌های تکراری (مدیریت مالی) با نوآوری‌های زیر:
- توصیه‌های مالی خودکار به‌صورت rule-based
- تشخیص خرج‌های تکراری
- محاسبه سرعت افزایش هزینه‌ها
- تاریخ خیالی رسیدن به اهداف پس‌انداز
- رمزنگاری سطح فیلد برای اطلاعات حساس

### داوری: معیار ۱۳ — قابلیت سفارشی‌سازی (۵ امتیاز)
- تغییر تم (تاریک/روشن)
- تنظیم مقیاس نمایش
- فعال/غیرفعال کردن نکات آموزشی
- حالت فشرده برای صفحه‌های کوچک
- فعال/غیرفعال حالت حریم خصوصی

### داوری: معیار ۱۴ — حجم بهینه و سرعت اجرا (۵ امتیاز)
حجم نصب بسیار کم (فقط یک فایل Python و یک دیتابیس SQLite). بدون وابستگی‌های سنگین. اجرا سریع به دلیل عدم اتصال به سرور.

### داوری: معیار ۱۵ — رعایت اصول کدنویسی پاک (۵ امتیاز)
- کامنت‌گذاری فقط بخش‌های پیچیده
- نام‌گذاری استاندارد (snake_case برای توابع/متغیرها، PascalCase برای کلاس‌ها)
- توابع هدفمند و کوتاه
- بدون کد تکراری (DRY)
- ساختار منطقی فایل‌ها

### داوری: معیار ۱۶ — مکانیسم‌های امنیتی (۵ امتیاز)
- هش پسورد با PBKDF2-HMAC-SHA256 (۲۰۰,۰۰۰ تکرار)
- رمزنگاری فیلد-level (XOR stream cipher)
- پارامتری‌سازی کوئری‌ها (SQL Injection prevention)
- ساختار داده جداگانه برای کاربران مختلف
- محیط آزمایش‌پذیر (تست‌های واحد موجود)

---

## بخش ۸: جمع‌بندی امتیاز

| معیار | ضریب | امتیاز |
|-------|-------|--------|
| ۱. مستندات مهندسی | 10 | خواه ارزیابی شود |
| ۲. ارزش علمی | 5 | خواه ارزیابی شود |
| ۳. راهنمای کاربر | 5 | خواه ارزیابی شود |
| ۴. جذابیت UI/UX | 10 | خواه ارزیابی شود |
| ۵. DApp/بلوکچین | 5 | خواه ارزیابی شود |
| ۶. مدیریت خطا و کدنویسی تمیز | 10 | خواه ارزیابی شود |
| ۷. چندپلتفرمه | 5 | خواه ارزیابی شود |
| ۸. پایگاه داده حرفه‌ای | 5 | خواه ارزیابی شود |
| ۹. مقیاس‌پذیری | 5 | خواه ارزیابی شود |
| ۱۰. تجاری‌سازی | 5 | خواه ارزیابی شود |
| ۱۱. بازخورد کاربر | 5 | خواه ارزیابی شود |
| ۱۲. خالقیت و نوآوری | 10 | خواه ارزیابی شود |
| ۱۳. سفارشی‌سازی | 5 | خواه ارزیابی شود |
| ۱۴. حجم و سرعت | 5 | خواه ارزیابی شود |
| ۱۵. کد پاک | 5 | خواه ارزیابی شود |
| ۱۶. امنیت | 5 | خواه ارزیابی شود |
| **جمع** | **100** | |

---

## بخش ۹: امضاها

| نام و نام خانوادگی داوران | امضا |
|---------------------------|-------|
| | |
| | |

| نام و نام خانوادگی ناظر | امضا |
|------------------------|-------|
| | |

| شناسه گروه | عنوان بازی |
|-----------|-----------|
| | Eghtesadino |

| حوزه کاربرد | سهمیه | تاریخ |
|------------|-------|-------|
| برنامه‌کاربردی دسکتاپ | | |
